image:
  repository: autodeploy-control-plane
  tag: ci

config:
  CONTROL_PLANE_ENV: production
  CONTROL_PLANE_EXECUTOR: fake

secrets:
  values:
    CONTROL_PLANE_DATABASE_URL: sqlite:////app/instance/control_plane.db
