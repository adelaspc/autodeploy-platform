apiVersion: v1
kind: Service
metadata:
  name: {{ include "autodeploy-control-plane.fullname" . }}
  labels:
    {{- include "autodeploy-control-plane.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "autodeploy-control-plane.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http

