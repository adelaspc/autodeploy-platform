{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "autodeploy-control-plane.serviceAccountName" . }}
  labels:
    {{- include "autodeploy-control-plane.labels" . | nindent 4 }}
{{- end }}

