[pytest]
testpaths =
    tests
norecursedirs =
    .git
    .pytest_cache
    .ruff_cache
    .venv
    .runtime
    demo-app
    frontend/dist
    frontend/node_modules
    instance
markers =
    docker: tests that require a local Docker daemon and are skipped unless Docker is available
